read comments at beginning of __init__.py file

This is a backup of my edits to mpl_toolkits basemap.

It's a workaround for now.

archived version is from when basemap got updated and I needed to match that version on the cluster, so I made a few changes